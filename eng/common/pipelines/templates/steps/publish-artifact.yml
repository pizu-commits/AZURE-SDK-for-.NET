# This Yaml Document has been converted by ESAI Yaml Pipeline Conversion Tool.
# Please make sure to check all the converted content, it is your team's responsibility to make sure that the pipeline is still valid and functions as expected.
# The Task 'PublishPipelineArtifact@1' has been converted to an output named 'Publish ${{ parameters.ArtifactName }} Artifacts' in the templateContext section.
# The Task 'PublishPipelineArtifact@1' has been converted to an output named 'Publish failed ${{ parameters.ArtifactName }} Artifacts' in the templateContext section.
parameters:
  ArtifactName: ''
  ArtifactPath: ''
  CustomCondition: true
steps: []