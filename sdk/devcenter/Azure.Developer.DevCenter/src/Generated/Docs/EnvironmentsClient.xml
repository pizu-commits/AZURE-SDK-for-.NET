<?xml version="1.0" encoding="utf-8"?>
<doc>
  <members>
    <member name="GetEnvironmentByUserAsync(String,String,RequestContext)">
<example>
This sample shows how to call GetEnvironmentByUserAsync with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = await client.GetEnvironmentByUserAsync("<environmentName>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.ToString());
]]></code>
This sample shows how to call GetEnvironmentByUserAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = await client.GetEnvironmentByUserAsync("<environmentName>", <me>);

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("user").ToString());
Console.WriteLine(result.GetProperty("provisioningState").ToString());
Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
Console.WriteLine(result.GetProperty("parameters").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>Environment</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentByUser(String,String,RequestContext)">
<example>
This sample shows how to call GetEnvironmentByUser with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = client.GetEnvironmentByUser("<environmentName>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.ToString());
]]></code>
This sample shows how to call GetEnvironmentByUser with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = client.GetEnvironmentByUser("<environmentName>", <me>);

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("user").ToString());
Console.WriteLine(result.GetProperty("provisioningState").ToString());
Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
Console.WriteLine(result.GetProperty("parameters").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>Environment</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="GetCatalogAsync(String,RequestContext)">
<example>
This sample shows how to call GetCatalogAsync with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = await client.GetCatalogAsync("<catalogName>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>Catalog</c>:
<code>{
  name: string, # Required. Name of the catalog.
}
</code>

</remarks>
    </member>
    <member name="GetCatalog(String,RequestContext)">
<example>
This sample shows how to call GetCatalog with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = client.GetCatalog("<catalogName>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>Catalog</c>:
<code>{
  name: string, # Required. Name of the catalog.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentDefinitionAsync(String,String,RequestContext)">
<example>
This sample shows how to call GetEnvironmentDefinitionAsync with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = await client.GetEnvironmentDefinitionAsync("<catalogName>", "<definitionName>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("description").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("description").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("default").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("type").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("readOnly").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("required").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("allowed")[0].ToString());
Console.WriteLine(result.GetProperty("parametersSchema").ToString());
Console.WriteLine(result.GetProperty("templatePath").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>EnvironmentDefinition</c>:
<code>{
  id: string, # Required. The ID of the environment definition.
  name: string, # Required. Name of the environment definition.
  catalogName: string, # Required. Name of the catalog.
  description: string, # Optional. A short description of the environment definition.
  parameters: [
    {
      id: string, # Required. Unique ID of the parameter
      name: string, # Optional. Display name of the parameter
      description: string, # Optional. Description of the parameter
      default: string, # Optional. Default value of the parameter
      type: &quot;array&quot; | &quot;boolean&quot; | &quot;integer&quot; | &quot;number&quot; | &quot;object&quot; | &quot;string&quot;, # Required. A string of one of the basic JSON types (number, integer, array, object, boolean, string)
      readOnly: boolean, # Optional. Whether or not this parameter is read-only.  If true, default should have a value.
      required: boolean, # Required. Whether or not this parameter is required
      allowed: [string], # Optional. An array of allowed values
    }
  ], # Optional. Input parameters passed to an environment.
  parametersSchema: string, # Optional. JSON schema defining the parameters object passed to an environment.
  templatePath: string, # Optional. Path to the Environment Definition entrypoint file.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentDefinition(String,String,RequestContext)">
<example>
This sample shows how to call GetEnvironmentDefinition with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

Response response = client.GetEnvironmentDefinition("<catalogName>", "<definitionName>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("description").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("description").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("default").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("type").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("readOnly").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("required").ToString());
Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("allowed")[0].ToString());
Console.WriteLine(result.GetProperty("parametersSchema").ToString());
Console.WriteLine(result.GetProperty("templatePath").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>EnvironmentDefinition</c>:
<code>{
  id: string, # Required. The ID of the environment definition.
  name: string, # Required. Name of the environment definition.
  catalogName: string, # Required. Name of the catalog.
  description: string, # Optional. A short description of the environment definition.
  parameters: [
    {
      id: string, # Required. Unique ID of the parameter
      name: string, # Optional. Display name of the parameter
      description: string, # Optional. Description of the parameter
      default: string, # Optional. Default value of the parameter
      type: &quot;array&quot; | &quot;boolean&quot; | &quot;integer&quot; | &quot;number&quot; | &quot;object&quot; | &quot;string&quot;, # Required. A string of one of the basic JSON types (number, integer, array, object, boolean, string)
      readOnly: boolean, # Optional. Whether or not this parameter is read-only.  If true, default should have a value.
      required: boolean, # Required. Whether or not this parameter is required
      allowed: [string], # Optional. An array of allowed values
    }
  ], # Optional. Input parameters passed to an environment.
  parametersSchema: string, # Optional. JSON schema defining the parameters object passed to an environment.
  templatePath: string, # Optional. Path to the Environment Definition entrypoint file.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentsAsync(Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentsAsync and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentsAsync())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call GetEnvironmentsAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentsAsync(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("user").ToString());
    Console.WriteLine(result.GetProperty("provisioningState").ToString());
    Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
    Console.WriteLine(result.GetProperty("parameters").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentListResultValue</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironments(Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironments and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironments())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call GetEnvironments with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironments(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("user").ToString());
    Console.WriteLine(result.GetProperty("provisioningState").ToString());
    Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
    Console.WriteLine(result.GetProperty("parameters").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentListResultValue</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentsByUserAsync(String,Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentsByUserAsync and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentsByUserAsync())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call GetEnvironmentsByUserAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentsByUserAsync(<me>, 1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("user").ToString());
    Console.WriteLine(result.GetProperty("provisioningState").ToString());
    Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
    Console.WriteLine(result.GetProperty("parameters").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentListResultValue</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentsByUser(String,Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentsByUser and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentsByUser())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call GetEnvironmentsByUser with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentsByUser(<me>, 1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("environmentType").ToString());
    Console.WriteLine(result.GetProperty("user").ToString());
    Console.WriteLine(result.GetProperty("provisioningState").ToString());
    Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
    Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
    Console.WriteLine(result.GetProperty("parameters").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentListResultValue</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="GetCatalogsByProjectAsync(Int32,RequestContext)">
<example>
This sample shows how to call GetCatalogsByProjectAsync and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetCatalogsByProjectAsync())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
}
]]></code>
This sample shows how to call GetCatalogsByProjectAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetCatalogsByProjectAsync(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>CatalogListResultValue</c>:
<code>{
  name: string, # Required. Name of the catalog.
}
</code>

</remarks>
    </member>
    <member name="GetCatalogsByProject(Int32,RequestContext)">
<example>
This sample shows how to call GetCatalogsByProject and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetCatalogsByProject())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
}
]]></code>
This sample shows how to call GetCatalogsByProject with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetCatalogsByProject(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>CatalogListResultValue</c>:
<code>{
  name: string, # Required. Name of the catalog.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentDefinitionsByProjectAsync(Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentDefinitionsByProjectAsync and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentDefinitionsByProjectAsync())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
}
]]></code>
This sample shows how to call GetEnvironmentDefinitionsByProjectAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentDefinitionsByProjectAsync(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("default").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("type").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("readOnly").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("required").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("allowed")[0].ToString());
    Console.WriteLine(result.GetProperty("parametersSchema").ToString());
    Console.WriteLine(result.GetProperty("templatePath").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentDefinitionListResultValue</c>:
<code>{
  id: string, # Required. The ID of the environment definition.
  name: string, # Required. Name of the environment definition.
  catalogName: string, # Required. Name of the catalog.
  description: string, # Optional. A short description of the environment definition.
  parameters: [
    {
      id: string, # Required. Unique ID of the parameter
      name: string, # Optional. Display name of the parameter
      description: string, # Optional. Description of the parameter
      default: string, # Optional. Default value of the parameter
      type: &quot;array&quot; | &quot;boolean&quot; | &quot;integer&quot; | &quot;number&quot; | &quot;object&quot; | &quot;string&quot;, # Required. A string of one of the basic JSON types (number, integer, array, object, boolean, string)
      readOnly: boolean, # Optional. Whether or not this parameter is read-only.  If true, default should have a value.
      required: boolean, # Required. Whether or not this parameter is required
      allowed: [string], # Optional. An array of allowed values
    }
  ], # Optional. Input parameters passed to an environment.
  parametersSchema: string, # Optional. JSON schema defining the parameters object passed to an environment.
  templatePath: string, # Optional. Path to the Environment Definition entrypoint file.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentDefinitionsByProject(Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentDefinitionsByProject and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentDefinitionsByProject())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
}
]]></code>
This sample shows how to call GetEnvironmentDefinitionsByProject with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentDefinitionsByProject(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("default").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("type").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("readOnly").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("required").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("allowed")[0].ToString());
    Console.WriteLine(result.GetProperty("parametersSchema").ToString());
    Console.WriteLine(result.GetProperty("templatePath").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentDefinitionListResultValue</c>:
<code>{
  id: string, # Required. The ID of the environment definition.
  name: string, # Required. Name of the environment definition.
  catalogName: string, # Required. Name of the catalog.
  description: string, # Optional. A short description of the environment definition.
  parameters: [
    {
      id: string, # Required. Unique ID of the parameter
      name: string, # Optional. Display name of the parameter
      description: string, # Optional. Description of the parameter
      default: string, # Optional. Default value of the parameter
      type: &quot;array&quot; | &quot;boolean&quot; | &quot;integer&quot; | &quot;number&quot; | &quot;object&quot; | &quot;string&quot;, # Required. A string of one of the basic JSON types (number, integer, array, object, boolean, string)
      readOnly: boolean, # Optional. Whether or not this parameter is read-only.  If true, default should have a value.
      required: boolean, # Required. Whether or not this parameter is required
      allowed: [string], # Optional. An array of allowed values
    }
  ], # Optional. Input parameters passed to an environment.
  parametersSchema: string, # Optional. JSON schema defining the parameters object passed to an environment.
  templatePath: string, # Optional. Path to the Environment Definition entrypoint file.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentDefinitionsByCatalogAsync(String,Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentDefinitionsByCatalogAsync with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentDefinitionsByCatalogAsync("<catalogName>"))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
}
]]></code>
This sample shows how to call GetEnvironmentDefinitionsByCatalogAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentDefinitionsByCatalogAsync("<catalogName>", 1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("default").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("type").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("readOnly").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("required").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("allowed")[0].ToString());
    Console.WriteLine(result.GetProperty("parametersSchema").ToString());
    Console.WriteLine(result.GetProperty("templatePath").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentDefinitionListResultValue</c>:
<code>{
  id: string, # Required. The ID of the environment definition.
  name: string, # Required. Name of the environment definition.
  catalogName: string, # Required. Name of the catalog.
  description: string, # Optional. A short description of the environment definition.
  parameters: [
    {
      id: string, # Required. Unique ID of the parameter
      name: string, # Optional. Display name of the parameter
      description: string, # Optional. Description of the parameter
      default: string, # Optional. Default value of the parameter
      type: &quot;array&quot; | &quot;boolean&quot; | &quot;integer&quot; | &quot;number&quot; | &quot;object&quot; | &quot;string&quot;, # Required. A string of one of the basic JSON types (number, integer, array, object, boolean, string)
      readOnly: boolean, # Optional. Whether or not this parameter is read-only.  If true, default should have a value.
      required: boolean, # Required. Whether or not this parameter is required
      allowed: [string], # Optional. An array of allowed values
    }
  ], # Optional. Input parameters passed to an environment.
  parametersSchema: string, # Optional. JSON schema defining the parameters object passed to an environment.
  templatePath: string, # Optional. Path to the Environment Definition entrypoint file.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentDefinitionsByCatalog(String,Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentDefinitionsByCatalog with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentDefinitionsByCatalog("<catalogName>"))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
}
]]></code>
This sample shows how to call GetEnvironmentDefinitionsByCatalog with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentDefinitionsByCatalog("<catalogName>", 1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("catalogName").ToString());
    Console.WriteLine(result.GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("description").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("default").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("type").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("readOnly").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("required").ToString());
    Console.WriteLine(result.GetProperty("parameters")[0].GetProperty("allowed")[0].ToString());
    Console.WriteLine(result.GetProperty("parametersSchema").ToString());
    Console.WriteLine(result.GetProperty("templatePath").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentDefinitionListResultValue</c>:
<code>{
  id: string, # Required. The ID of the environment definition.
  name: string, # Required. Name of the environment definition.
  catalogName: string, # Required. Name of the catalog.
  description: string, # Optional. A short description of the environment definition.
  parameters: [
    {
      id: string, # Required. Unique ID of the parameter
      name: string, # Optional. Display name of the parameter
      description: string, # Optional. Description of the parameter
      default: string, # Optional. Default value of the parameter
      type: &quot;array&quot; | &quot;boolean&quot; | &quot;integer&quot; | &quot;number&quot; | &quot;object&quot; | &quot;string&quot;, # Required. A string of one of the basic JSON types (number, integer, array, object, boolean, string)
      readOnly: boolean, # Optional. Whether or not this parameter is read-only.  If true, default should have a value.
      required: boolean, # Required. Whether or not this parameter is required
      allowed: [string], # Optional. An array of allowed values
    }
  ], # Optional. Input parameters passed to an environment.
  parametersSchema: string, # Optional. JSON schema defining the parameters object passed to an environment.
  templatePath: string, # Optional. Path to the Environment Definition entrypoint file.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentTypesAsync(Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentTypesAsync and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentTypesAsync())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("deploymentTargetId").ToString());
    Console.WriteLine(result.GetProperty("status").ToString());
}
]]></code>
This sample shows how to call GetEnvironmentTypesAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

await foreach (var data in client.GetEnvironmentTypesAsync(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("deploymentTargetId").ToString());
    Console.WriteLine(result.GetProperty("status").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentTypeListResultValue</c>:
<code>{
  name: string, # Required. Name of the environment type
  deploymentTargetId: string, # Required. Id of a subscription or management group that the environment type will be mapped to. The environment&apos;s resources will be deployed into this subscription or management group.
  status: &quot;Enabled&quot; | &quot;Disabled&quot;, # Required. Indicates whether this environment type is enabled for use in this project.
}
</code>

</remarks>
    </member>
    <member name="GetEnvironmentTypes(Int32,RequestContext)">
<example>
This sample shows how to call GetEnvironmentTypes and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentTypes())
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("deploymentTargetId").ToString());
    Console.WriteLine(result.GetProperty("status").ToString());
}
]]></code>
This sample shows how to call GetEnvironmentTypes with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

foreach (var data in client.GetEnvironmentTypes(1234))
{
    JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("deploymentTargetId").ToString());
    Console.WriteLine(result.GetProperty("status").ToString());
}
]]></code>
</example>
<remarks>
Below is the JSON schema for one item in the pageable response.

Response Body:

Schema for <c>EnvironmentTypeListResultValue</c>:
<code>{
  name: string, # Required. Name of the environment type
  deploymentTargetId: string, # Required. Id of a subscription or management group that the environment type will be mapped to. The environment&apos;s resources will be deployed into this subscription or management group.
  status: &quot;Enabled&quot; | &quot;Disabled&quot;, # Required. Indicates whether this environment type is enabled for use in this project.
}
</code>

</remarks>
    </member>
    <member name="CreateOrReplaceEnvironmentAsync(WaitUntil,String,RequestContent,String,RequestContext)">
<example>
This sample shows how to call CreateOrReplaceEnvironmentAsync with required parameters and request content, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var data = new {
    environmentType = "<environmentType>",
    catalogName = "<catalogName>",
    environmentDefinitionName = "<environmentDefinitionName>",
};

var operation = await client.CreateOrReplaceEnvironmentAsync(WaitUntil.Completed, "<environmentName>", RequestContent.Create(data));

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.ToString());
]]></code>
This sample shows how to call CreateOrReplaceEnvironmentAsync with all parameters and request content, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var data = new {
    environmentType = "<environmentType>",
    catalogName = "<catalogName>",
    environmentDefinitionName = "<environmentDefinitionName>",
    parameters = new {},
};

var operation = await client.CreateOrReplaceEnvironmentAsync(WaitUntil.Completed, "<environmentName>", RequestContent.Create(data), <me>);

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("user").ToString());
Console.WriteLine(result.GetProperty("provisioningState").ToString());
Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
Console.WriteLine(result.GetProperty("parameters").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the request and response payloads.

Request Body:

Schema for <c>Environment</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

Response Body:

Schema for <c>Environment</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="CreateOrReplaceEnvironment(WaitUntil,String,RequestContent,String,RequestContext)">
<example>
This sample shows how to call CreateOrReplaceEnvironment with required parameters and request content, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var data = new {
    environmentType = "<environmentType>",
    catalogName = "<catalogName>",
    environmentDefinitionName = "<environmentDefinitionName>",
};

var operation = client.CreateOrReplaceEnvironment(WaitUntil.Completed, "<environmentName>", RequestContent.Create(data));

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.ToString());
]]></code>
This sample shows how to call CreateOrReplaceEnvironment with all parameters and request content, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var data = new {
    environmentType = "<environmentType>",
    catalogName = "<catalogName>",
    environmentDefinitionName = "<environmentDefinitionName>",
    parameters = new {},
};

var operation = client.CreateOrReplaceEnvironment(WaitUntil.Completed, "<environmentName>", RequestContent.Create(data), <me>);

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("environmentType").ToString());
Console.WriteLine(result.GetProperty("user").ToString());
Console.WriteLine(result.GetProperty("provisioningState").ToString());
Console.WriteLine(result.GetProperty("resourceGroupId").ToString());
Console.WriteLine(result.GetProperty("catalogName").ToString());
Console.WriteLine(result.GetProperty("environmentDefinitionName").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("target").ToString());
Console.WriteLine(result.GetProperty("parameters").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the request and response payloads.

Request Body:

Schema for <c>Environment</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

Response Body:

Schema for <c>Environment</c>:
<code>{
  name: string, # Optional. Environment name.
  environmentType: string, # Required. Environment type.
  user: string, # Optional. The AAD object id of the owner of this Environment.
  provisioningState: string, # Optional. The provisioning state of the environment.
  resourceGroupId: string, # Optional. The identifier of the resource group containing the environment&apos;s resources.
  catalogName: string, # Required. Name of the catalog.
  environmentDefinitionName: string, # Required. Name of the environment definition.
  error: {
    code: string, # Required. An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
    message: string, # Required. A message describing the error, intended to be suitable for display in a user interface.
    target: string, # Optional. The target of the particular error. For example, the name of the property in error.
    details: [CloudErrorBody], # Optional. A list of additional details about the error.
  }, # Optional. Provisioning error details. Populated only for error states.
  parameters: AnyObject, # Optional. Parameters object for the environment.
}
</code>

</remarks>
    </member>
    <member name="DeleteEnvironmentAsync(WaitUntil,String,String,RequestContext)">
<example>
This sample shows how to call DeleteEnvironmentAsync with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var operation = await client.DeleteEnvironmentAsync(WaitUntil.Completed, "<environmentName>");

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("status").ToString());
]]></code>
This sample shows how to call DeleteEnvironmentAsync with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var operation = await client.DeleteEnvironmentAsync(WaitUntil.Completed, "<environmentName>", <me>);

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("status").ToString());
Console.WriteLine(result.GetProperty("resourceId").ToString());
Console.WriteLine(result.GetProperty("startTime").ToString());
Console.WriteLine(result.GetProperty("endTime").ToString());
Console.WriteLine(result.GetProperty("percentComplete").ToString());
Console.WriteLine(result.GetProperty("properties").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>OperationStatus</c>:
<code>{
  id: string, # Optional. Fully qualified ID for the operation status.
  name: string, # Optional. The operation id name
  status: string, # Required. Provisioning state of the resource.
  resourceId: string, # Optional. The id of the resource.
  startTime: string (ISO 8601 Format), # Optional. The start time of the operation
  endTime: string (ISO 8601 Format), # Optional. The end time of the operation
  percentComplete: number, # Optional. Percent of the operation that is complete
  properties: AnyObject, # Optional. Custom operation properties, populated only for a successful operation.
  error: {
    code: string, # Optional. The error code.
    message: string, # Optional. The error message.
  }, # Optional. Operation Error message
}
</code>

</remarks>
    </member>
    <member name="DeleteEnvironment(WaitUntil,String,String,RequestContext)">
<example>
This sample shows how to call DeleteEnvironment with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var operation = client.DeleteEnvironment(WaitUntil.Completed, "<environmentName>");

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("status").ToString());
]]></code>
This sample shows how to call DeleteEnvironment with all parameters, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new EnvironmentsClient(endpoint, "<projectName>", credential);

var operation = client.DeleteEnvironment(WaitUntil.Completed, "<environmentName>", <me>);

BinaryData data = operation.Value;
JsonElement result = JsonDocument.Parse(data.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("status").ToString());
Console.WriteLine(result.GetProperty("resourceId").ToString());
Console.WriteLine(result.GetProperty("startTime").ToString());
Console.WriteLine(result.GetProperty("endTime").ToString());
Console.WriteLine(result.GetProperty("percentComplete").ToString());
Console.WriteLine(result.GetProperty("properties").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("error").GetProperty("message").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>OperationStatus</c>:
<code>{
  id: string, # Optional. Fully qualified ID for the operation status.
  name: string, # Optional. The operation id name
  status: string, # Required. Provisioning state of the resource.
  resourceId: string, # Optional. The id of the resource.
  startTime: string (ISO 8601 Format), # Optional. The start time of the operation
  endTime: string (ISO 8601 Format), # Optional. The end time of the operation
  percentComplete: number, # Optional. Percent of the operation that is complete
  properties: AnyObject, # Optional. Custom operation properties, populated only for a successful operation.
  error: {
    code: string, # Optional. The error code.
    message: string, # Optional. The error message.
  }, # Optional. Operation Error message
}
</code>

</remarks>
    </member>
  </members>
</doc>